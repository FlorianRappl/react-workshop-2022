import * as React from 'react';
import TodoList from './todoList';
import { render } from 'react-dom';

// Edit todoList.jsx and todoListItem.jsx to solve the assignment

render(
  <TodoList />,
  document.querySelector('#app')
);
